# --- Framework API ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# --- Configuration & validation ---
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Base de donnees ---
sqlalchemy==2.0.31
alembic==1.13.2

# --- Authentification (Keycloak / JWT) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.27.0

# --- LLM / Embeddings ---
# Ollama : pas de SDK dedie, appels REST via httpx (deja liste ci-dessus)

# --- Vector Store ---
chromadb>=0.6.0
numpy>=2.1.0

# --- Traitement de documents ---
pypdf==4.2.0
python-docx==1.1.2
markdown==3.6

# --- Utilitaires ---
tenacity==8.4.1
loguru==0.7.2
PyYAML==6.0.1
python-slugify==8.0.4








